{
  "name": "telegram-session-generator",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build"
  },
  "dependencies": {
    "telegram": "^2.20.2"
  }
}